# 修复 Web 版功能缺失问题

根据您的反馈和代码检查，我发现之前添加的“注销账号”按钮虽然代码逻辑已存在，但在最终的界面渲染部分未能成功应用，导致在网页版（以及移动版）上都看不到该按钮。

## 修复计划

### 1. 修复 `Settings.tsx` 界面渲染
*   **添加“注销账号”按钮**: 在设置页底部，将“注销账号”按钮重新添加到 UI 中。
*   **优化按钮样式**:
    *   将现有的“清空所有记录”按钮样式调整为 **橙色** (Orange)，以表示警告。
    *   新增的“注销账号”按钮使用 **红色** (Red)，以表示危险操作。
*   **验证功能**: 确保 `handleDeleteAccount` 函数被正确调用。

### 2. 确认 Web 版兼容性
*   **账号注销**: 该功能基于 Supabase 和后端 API，天然支持 Web 版。
*   **隐私政策**: 使用 `window.open` 跳转，天然支持 Web 版。
*   **数据导出/导入**: 代码中已包含 `if (!Capacitor.isNativePlatform())` 的 Web 回退逻辑，确保在浏览器中也能导出 TXT 文件。

执行完上述修复后，网页版将具备与 App 版完全一致的账号管理和隐私合规能力。
